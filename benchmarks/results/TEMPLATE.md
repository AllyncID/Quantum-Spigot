# Benchmark observation

Status: NOT RUN

* Variant / exact commits / artifact SHA-256:
* Java version / GC / heap / JVM arguments:
* CPU / RAM / OS / storage / power mode:
* Scenario / seed / fixture hash / player script:
* Plugin names and exact versions:
* View distance / simulation distance / loaded chunks / entities:
* Warmup / sample duration / repetition:
* Raw CSV / JFR or Spark evidence:
* p50 / p95 / p99 / max MSPT and measurement boundary:
* Main-thread / total CPU / allocation / GC pause observations:
* Chunk load/send / login / teleport / chat latency:
* Correctness and restart observations:
* Missing measurements / environmental interference:

Conclusion: no performance claim until repeated matched results exist.
